version: 2.1

jobs:
  build:
    docker:
    - image: fedora:latest
    steps:
    - run: 
        command: "sudo dnf update"
        name: update

workflows:
  build:
    jobs:
    - build
  version: 2
